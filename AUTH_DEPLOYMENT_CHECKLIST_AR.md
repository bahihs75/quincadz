# قائمة تحقق إصلاح تسجيل الدخول والنشر

## ما تم إصلاحه في الكود

تستخدم صفحة تسجيل الدخول الآن `public/logo.svg` الموجود فعلياً بدلاً من `/logo.png` غير الموجود. كما تم تحديث صفحة التسجيل إلى المسار نفسه، وتوحيد ألوان الشعار مع `#F5C400` و`#111111` و`#FFFFFF`.

تلتقط صفحة الدخول أخطاء الشبكة والاستثناءات عبر `try/catch/finally`. عند ظهور رسائل مثل `Load failed` أو `Failed to fetch`، تعرض الصفحة رسالة عربية تشرح أن الاتصال بخدمة المصادقة تعذر، وتعيد الزر إلى حالته الطبيعية بدلاً من تركه في تحميل دائم.

## إعدادات Vercel المطلوبة

| المتغير | القيمة المطلوبة |
|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | رابط مشروع Supabase الصحيح، مثل `https://project-ref.supabase.co` |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | المفتاح العام anon/publishable الخاص بالمشروع نفسه |

لا تضع `service_role` في متغير يبدأ بـ `NEXT_PUBLIC_` ولا تضعه داخل كود المتصفح. بعد إضافة أو تعديل المتغيرات، يجب تنفيذ Redeploy للبيئة التي تخدم `quincadz.vercel.app`.

## إعدادات Supabase المطلوبة

في لوحة Supabase، يجب أن يكون عنوان الموقع المنشور مضافاً ضمن Site URL أو Redirect URLs حسب إعدادات المشروع:

```text
https://quincadz.vercel.app
https://quincadz.vercel.app/auth/callback
```

إذا كان للمشروع نطاق إنتاج آخر، يضاف نطاقه مع مسار callback نفسه. يجب تفعيل Email provider إذا كان تسجيل الدخول بالبريد وكلمة المرور مطلوباً، وتفعيل Google provider فقط بعد ضبط Client ID وClient Secret وإضافة رابط callback الصحيح لدى Google Cloud.

## خطوات النشر

أولاً، ادمج Pull Request رقم 1 في الفرع المتصل بمشروع Vercel أو نفّذ نشر الفرع `manus/quincadz-review` إذا كان المشروع مضبوطاً على نشر الفروع. ثانياً، تأكد من أن آخر commit المنشور هو `96debc6` أو أحدث. ثالثاً، نفّذ Redeploy مع خيار مسح الذاكرة المؤقتة عند الحاجة. رابعاً، افتح صفحة الدخول في نافذة خاصة وتحقق من أن المورد هو `/logo.svg` وأن النص أصبح عربياً.

لا تختبر بإرسال بيانات حساسة في سجل عام أو مشاركة كلمة المرور. الاختبار الصحيح هو حساب تجريبي مخصص، مع التحقق من حالتين: بيانات صحيحة، وبريد أو كلمة مرور غير صحيحة. يجب أن تعرض الحالة الثانية رسالة مفهومة وألا تكشف تفاصيل داخلية من Supabase.

## تفسير الحالة الحالية

وقت الفحص، كان `quincadz.vercel.app` يعرض النسخة القديمة التي تستدعي `/logo.png` وتستخدم النص الإنجليزي واللون البرتقالي. هذا يعني أن إصلاح الكود موجود في فرع GitHub، لكن النطاق العام لم يتلق آخر deployment بعد. إذا بقيت الصفحة القديمة بعد دمج Pull Request، فالمشكلة في ربط Vercel بالفرع أو في إعدادات deployment، وليست في مرجع الشعار الجديد داخل الكود.
